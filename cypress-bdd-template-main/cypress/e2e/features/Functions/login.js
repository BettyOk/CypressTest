import { Given, When, Then } from "cypress-cucumber-preprocessor/steps";
 
Then('Then The user should be redirected to the course page', () => {
	cy.Pending();
});

Then('Then The user should be redirected to the course page', () => {
	cy.Pending();
});
